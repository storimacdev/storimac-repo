import OnboardingFlow from "@/components/OnboardingFlow";

export const metadata = {
  title: "Set up your workspace — Storimac",
};

export default function OnboardingPage() {
  return <OnboardingFlow />;
}
